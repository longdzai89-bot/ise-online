{
  "name": "ise-online",
  "version": "1.0.0",
  "description": "ISE Online - Pixel Platformer Game",
  "main": "index.js",
  "scripts": {
    "serve": "npx serve game -p 3000",
    "build:android": "cd android && ./gradlew assembleDebug",
    "sync": "npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^5.7.0",
    "@capacitor/core": "^5.7.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.7.0"
  }
}
